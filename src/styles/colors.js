'use strict';

var React = require('react-native');
var {StyleSheet} = React;

var colors = StyleSheet.create({
  white: {
    color: '#fff',
  },
  blue: {
    color: '#09c',
  },
  grey: {
    color: '#ccc',
  },
});

module.exports = colors;
